// The factorial problem involves calculating the factorial of a positive
// integer 'n'. The factorial of a non-negative integer 'n', denoted as 'n!',
// is the product of all positive integers from '1' to 'n'.
//
// n! = n * (n - 1) * (n - 2) * ... * 2 * 1
//
// Assume all input to this function is >= 0

const factorial = (n) => {
  // Define the base case.
  // Define the recursive case.
};

export default factorial;
